# Custom-U-NET-Segmentations
# U-Net Image Segmentation  PyTorch U-Net for semantic segmentation with automatic dataset analysis, flexible mask support (RGB/grayscale), GPU acceleration, real-time training visualization, IoU metrics, and easy prediction pipeline. Auto-detects classes and remaps mask values.
